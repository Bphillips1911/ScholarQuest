// src/pages/acap/ImpactSimulatorPage.tsx
import * as React from "react";
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Separator } from "@/components/ui/separator";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Input } from "@/components/ui/input";
import { Slider } from "@/components/ui/slider";
import { ScrollArea } from "@/components/ui/scroll-area";
import { Progress } from "@/components/ui/progress";
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter } from "@/components/ui/dialog";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@/components/ui/tooltip";
import {
  ArrowUpRight,
  Download,
  Sparkles,
  Wand2,
  Target,
  Timer,
  Users,
  PlayCircle,
  FileText,
  Table2,
  SlidersHorizontal,
} from "lucide-react";

/**
 * WIRING NOTES (Agent will implement):
 * - Fetch current projected score + distributions from /api/acap/projected-score (or your existing endpoint)
 * - Run simulator: POST /api/acap/impact/run
 * - Export: POST /api/acap/impact/export/pdf | /csv
 */

type ScopeType = "SCHOOL" | "GRADE" | "SUBJECT" | "CLASS";
type Subject = "ELA" | "MATH" | "SCI";

type ImpactAction =
  | { type: "ASSIGN_BOOTCAMP"; payload: Record<string, any> }
  | { type: "GENERATE_ITEMSET"; payload: Record<string, any> }
  | { type: "CREATE_MICROASSESS"; payload: Record<string, any> }
  | { type: "SCHEDULE_COACHING"; payload: Record<string, any> };

type Lever = {
  id: string;
  name: string;
  leverType: string;
  estimatedPointGain: number;
  weeksToImpact: number;
  studentsAffected: number;
  confidence: number; // 0-1
  summary: string;
  action?: ImpactAction;
};

function formatGain(n: number) {
  const sign = n >= 0 ? "+" : "";
  return `${sign}${n.toFixed(1)}`;
}

export default function ImpactSimulatorPage() {
  // ---------- State (wire to backend) ----------
  const [scopeType, setScopeType] = React.useState<ScopeType>("SCHOOL");
  const [subject, setSubject] = React.useState<Subject>("MATH");
  const [grade, setGrade] = React.useState<string>("6");
  const [classId, setClassId] = React.useState<string>("all");

  const [dateRange, setDateRange] = React.useState<"30d" | "60d" | "90d" | "qtr">("qtr");

  const [targetLetter, setTargetLetter] = React.useState<"A" | "B" | "C" | "D">("B");
  const [attendancePoints, setAttendancePoints] = React.useState<number>(10.5);
  const [elPoints, setElPoints] = React.useState<number>(0);

  // What-if controls (optional)
  const [dok34Lift, setDok34Lift] = React.useState<number[]>([15]); // percent lift
  const [writingEvidenceLift, setWritingEvidenceLift] = React.useState<number[]>([10]); // percent lift

  // Data placeholders (Agent will replace with fetched values)
  const [currentProjectedScore, setCurrentProjectedScore] = React.useState<number>(83);
  const [currentLetter, setCurrentLetter] = React.useState<string>("B");
  const [projectedPointGain, setProjectedPointGain] = React.useState<number>(9.4);

  const [levers, setLevers] = React.useState<Lever[]>([
    {
      id: "lev1",
      name: "Boost Math DOK 3–4 Instruction",
      leverType: "DOK_SHIFT",
      estimatedPointGain: 3.8,
      weeksToImpact: 6,
      studentsAffected: 63,
      confidence: 0.72,
      summary: "Move DOK3 accuracy in high-weight domains; increases L2→L3 conversions.",
      action: { type: "ASSIGN_BOOTCAMP", payload: { track: "MATH_DOK3_PROPORTIONAL_REASONING", durationWeeks: 4 } },
    },
    {
      id: "lev2",
      name: "Improve Text Evidence Scores",
      leverType: "WRITING_EVIDENCE",
      estimatedPointGain: 2.7,
      weeksToImpact: 5,
      studentsAffected: 56,
      confidence: 0.66,
      summary: "Raise evidence+reasoning subscore on rubric; improves WritingIndex and proficiency movement.",
      action: { type: "SCHEDULE_COACHING", payload: { focus: "EVIDENCE_REASONING", durationWeeks: 4 } },
    },
    {
      id: "lev3",
      name: "Strengthen Vocabulary Stamina",
      leverType: "VOCAB",
      estimatedPointGain: 2.2,
      weeksToImpact: 8,
      studentsAffected: 61,
      confidence: 0.59,
      summary: "Reduce performance drop on medium/high vocab-load items; increases comprehension and DOK3 success.",
      action: { type: "GENERATE_ITEMSET", payload: { subject: "ELA", grade: 6, dok: [2, 3], domain: "Vocabulary", itemCount: 30 } },
    },
  ]);

  const [isRunning, setIsRunning] = React.useState(false);
  const [openExport, setOpenExport] = React.useState(false);

  // ---------- Handlers (Agent wiring) ----------
  async function runSimulator() {
    setIsRunning(true);
    try {
      // TODO: Agent wires:
      // const res = await fetch("/api/acap/impact/run", { method:"POST", body: JSON.stringify({...}) })
      // const json = await res.json()
      // setLevers(json.topLevers)
      // setProjectedPointGain(json.projectedPointGain)
      await new Promise((r) => setTimeout(r, 650));
    } finally {
      setIsRunning(false);
    }
  }

  function onAction(action?: ImpactAction) {
    if (!action) return;
    // TODO: Agent wires to existing flows:
    // - open Assign Boot Camp modal
    // - open Generate Item Set modal
    // - open Create Micro Assessment modal
    // - open Schedule Coaching modal / create task
    console.log("Action:", action);
  }

  return (
    <TooltipProvider>
      <div className="min-h-screen bg-slate-50">
        {/* Page Header */}
        <div className="border-b bg-white">
          <div className="mx-auto max-w-7xl px-4 py-5">
            <div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
              <div>
                <h1 className="text-2xl font-semibold tracking-tight">Instructional Impact Simulator™</h1>
                <p className="text-sm text-muted-foreground">
                  Identify the highest-leverage instructional moves to raise your projected ACAP report score—fast.
                </p>
              </div>

              <div className="flex flex-wrap items-center gap-2">
                <Select value={dateRange} onValueChange={(v) => setDateRange(v as any)}>
                  <SelectTrigger className="w-[160px]">
                    <SelectValue placeholder="Date range" />
                  </SelectTrigger>
                  <SelectContent>
                    <SelectItem value="30d">Last 30 days</SelectItem>
                    <SelectItem value="60d">Last 60 days</SelectItem>
                    <SelectItem value="90d">Last 90 days</SelectItem>
                    <SelectItem value="qtr">This quarter</SelectItem>
                  </SelectContent>
                </Select>

                <Button onClick={runSimulator} disabled={isRunning} className="gap-2">
                  <Sparkles className="h-4 w-4" />
                  {isRunning ? "Running…" : "Run Simulator"}
                </Button>

                <DropdownMenu>
                  <DropdownMenuTrigger asChild>
                    <Button variant="outline" className="gap-2">
                      <Download className="h-4 w-4" />
                      Export
                    </Button>
                  </DropdownMenuTrigger>
                  <DropdownMenuContent align="end">
                    <DropdownMenuItem onClick={() => setOpenExport(true)} className="gap-2">
                      <FileText className="h-4 w-4" />
                      Export PDF
                    </DropdownMenuItem>
                    <DropdownMenuItem onClick={() => setOpenExport(true)} className="gap-2">
                      <Table2 className="h-4 w-4" />
                      Export CSV
                    </DropdownMenuItem>
                  </DropdownMenuContent>
                </DropdownMenu>
              </div>
            </div>

            {/* Scope Bar */}
            <div className="mt-4 grid grid-cols-1 gap-3 md:grid-cols-12">
              <Card className="md:col-span-8">
                <CardContent className="p-4">
                  <div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
                    <div className="flex flex-wrap items-center gap-2">
                      <Badge variant="secondary" className="gap-1">
                        <SlidersHorizontal className="h-3.5 w-3.5" />
                        Scope
                      </Badge>

                      <Select value={scopeType} onValueChange={(v) => setScopeType(v as ScopeType)}>
                        <SelectTrigger className="w-[150px]">
                          <SelectValue placeholder="Scope" />
                        </SelectTrigger>
                        <SelectContent>
                          <SelectItem value="SCHOOL">School</SelectItem>
                          <SelectItem value="GRADE">Grade</SelectItem>
                          <SelectItem value="SUBJECT">Subject</SelectItem>
                          <SelectItem value="CLASS">Class</SelectItem>
                        </SelectContent>
                      </Select>

                      <Select value={subject} onValueChange={(v) => setSubject(v as Subject)}>
                        <SelectTrigger className="w-[150px]">
                          <SelectValue placeholder="Subject" />
                        </SelectTrigger>
                        <SelectContent>
                          <SelectItem value="ELA">ELA</SelectItem>
                          <SelectItem value="MATH">Math</SelectItem>
                          <SelectItem value="SCI">Science</SelectItem>
                        </SelectContent>
                      </Select>

                      <Select value={grade} onValueChange={setGrade}>
                        <SelectTrigger className="w-[120px]">
                          <SelectValue placeholder="Grade" />
                        </SelectTrigger>
                        <SelectContent>
                          {["6", "7", "8"].map((g) => (
                            <SelectItem key={g} value={g}>
                              Grade {g}
                            </SelectItem>
                          ))}
                        </SelectContent>
                      </Select>

                      <Select value={classId} onValueChange={setClassId}>
                        <SelectTrigger className="w-[180px]">
                          <SelectValue placeholder="Class" />
                        </SelectTrigger>
                        <SelectContent>
                          <SelectItem value="all">All Classes</SelectItem>
                          <SelectItem value="p1">Period 1</SelectItem>
                          <SelectItem value="p2">Period 2</SelectItem>
                          <SelectItem value="p3">Period 3</SelectItem>
                        </SelectContent>
                      </Select>
                    </div>

                    <div className="flex flex-wrap items-center gap-2">
                      <div className="flex items-center gap-2 rounded-lg border bg-white px-3 py-2">
                        <span className="text-xs text-muted-foreground">Attendance pts</span>
                        <Input
                          type="number"
                          step="0.1"
                          value={attendancePoints}
                          onChange={(e) => setAttendancePoints(Number(e.target.value))}
                          className="h-8 w-[90px]"
                        />
                      </div>
                      <div className="flex items-center gap-2 rounded-lg border bg-white px-3 py-2">
                        <span className="text-xs text-muted-foreground">EL pts</span>
                        <Input
                          type="number"
                          step="0.1"
                          value={elPoints}
                          onChange={(e) => setElPoints(Number(e.target.value))}
                          className="h-8 w-[90px]"
                        />
                      </div>
                      <Select value={targetLetter} onValueChange={(v) => setTargetLetter(v as any)}>
                        <SelectTrigger className="w-[150px]">
                          <SelectValue placeholder="Target grade" />
                        </SelectTrigger>
                        <SelectContent>
                          <SelectItem value="A">Target A</SelectItem>
                          <SelectItem value="B">Target B</SelectItem>
                          <SelectItem value="C">Target C</SelectItem>
                          <SelectItem value="D">Target D</SelectItem>
                        </SelectContent>
                      </Select>
                    </div>
                  </div>
                </CardContent>
              </Card>

              {/* Big KPI Tile */}
              <Card className="md:col-span-4">
                <CardHeader className="pb-2">
                  <CardTitle className="text-sm font-medium text-muted-foreground">Projected Point Gain</CardTitle>
                  <CardDescription className="text-xs">Based on your current projection model + levers</CardDescription>
                </CardHeader>
                <CardContent className="pt-0">
                  <div className="flex items-end justify-between">
                    <div className="flex items-baseline gap-2">
                      <div className="text-4xl font-semibold">{formatGain(projectedPointGain)}</div>
                      <Badge className="mb-1" variant="secondary">
                        {currentProjectedScore} {currentLetter}
                      </Badge>
                    </div>
                    <div className="flex items-center gap-2 text-xs text-muted-foreground">
                      <Target className="h-4 w-4" />
                      Target {targetLetter}
                    </div>
                  </div>
                  <Separator className="my-3" />
                  <div className="grid grid-cols-2 gap-3 text-sm">
                    <div className="rounded-lg border bg-white p-3">
                      <div className="text-xs text-muted-foreground">Current score</div>
                      <div className="mt-1 text-lg font-semibold">{currentProjectedScore}</div>
                    </div>
                    <div className="rounded-lg border bg-white p-3">
                      <div className="text-xs text-muted-foreground">Projected score</div>
                      <div className="mt-1 text-lg font-semibold">{Math.round(currentProjectedScore + projectedPointGain)}</div>
                    </div>
                  </div>
                </CardContent>
              </Card>
            </div>
          </div>
        </div>

        {/* Content */}
        <div className="mx-auto max-w-7xl px-4 py-6">
          <div className="grid grid-cols-1 gap-6 lg:grid-cols-12">
            {/* Left: Top Levers */}
            <Card className="lg:col-span-8">
              <CardHeader>
                <CardTitle>Top 3 Instructional Levers This Quarter</CardTitle>
                <CardDescription>High-impact moves ranked by point gain, time to impact, and cohort size.</CardDescription>
              </CardHeader>
              <CardContent className="space-y-4">
                {levers.map((l) => (
                  <LeverCard key={l.id} lever={l} onAction={() => onAction(l.action)} />
                ))}

                <Separator className="my-2" />

                <div className="flex flex-wrap items-center justify-between gap-2">
                  <div className="text-sm text-muted-foreground">
                    Tip: Launch one lever for 4–6 weeks, then rerun to confirm realized gains.
                  </div>
                  <Button variant="outline" className="gap-2">
                    <Wand2 className="h-4 w-4" />
                    Create Action Plan
                  </Button>
                </div>
              </CardContent>
            </Card>

            {/* Right: Controls / What-if */}
            <Card className="lg:col-span-4">
              <CardHeader>
                <CardTitle>What-if Controls</CardTitle>
                <CardDescription>Preview adjustments before committing to a lever.</CardDescription>
              </CardHeader>
              <CardContent className="space-y-5">
                <div className="space-y-2">
                  <div className="flex items-center justify-between">
                    <div className="text-sm font-medium">DOK 3–4 accuracy lift</div>
                    <Badge variant="secondary">+{dok34Lift[0]}%</Badge>
                  </div>
                  <Slider value={dok34Lift} onValueChange={setDok34Lift} max={30} step={1} />
                  <p className="text-xs text-muted-foreground">
                    Used in DOK_SHIFT lever simulations. Agent will map this to projected score delta.
                  </p>
                </div>

                <div className="space-y-2">
                  <div className="flex items-center justify-between">
                    <div className="text-sm font-medium">Writing evidence lift</div>
                    <Badge variant="secondary">+{writingEvidenceLift[0]}%</Badge>
                  </div>
                  <Slider value={writingEvidenceLift} onValueChange={setWritingEvidenceLift} max={30} step={1} />
                  <p className="text-xs text-muted-foreground">
                    Used in WRITING_EVIDENCE simulations to raise WritingIndex (rubric evidence/reasoning).
                  </p>
                </div>

                <Separator />

                <StudentsNeededCard
                  currentScore={currentProjectedScore}
                  currentLetter={currentLetter}
                  targetLetter={targetLetter}
                />

                <Separator />

                <div className="flex flex-wrap gap-2">
                  <Button className="flex-1 gap-2" onClick={runSimulator} disabled={isRunning}>
                    <PlayCircle className="h-4 w-4" />
                    Run
                  </Button>
                  <Button variant="outline" className="flex-1 gap-2" onClick={() => setOpenExport(true)}>
                    <Download className="h-4 w-4" />
                    Export
                  </Button>
                </div>
              </CardContent>
            </Card>
          </div>
        </div>

        {/* Export Dialog */}
        <Dialog open={openExport} onOpenChange={setOpenExport}>
          <DialogContent className="sm:max-w-[520px]">
            <DialogHeader>
              <DialogTitle>Export Impact Summary</DialogTitle>
              <DialogDescription>Export your top levers and assumptions for leadership teams.</DialogDescription>
            </DialogHeader>

            <div className="space-y-3">
              <Card>
                <CardContent className="p-4 text-sm">
                  <div className="flex items-center justify-between">
                    <div className="font-medium">PDF (Executive Summary)</div>
                    <Button variant="outline" size="sm" className="gap-2">
                      <FileText className="h-4 w-4" />
                      Export
                    </Button>
                  </div>
                  <p className="mt-1 text-xs text-muted-foreground">
                    Includes KPI tile, top 3 levers, students affected, timeline, and action plan.
                  </p>
                </CardContent>
              </Card>

              <Card>
                <CardContent className="p-4 text-sm">
                  <div className="flex items-center justify-between">
                    <div className="font-medium">CSV (Data + Assumptions)</div>
                    <Button variant="outline" size="sm" className="gap-2">
                      <Table2 className="h-4 w-4" />
                      Export
                    </Button>
                  </div>
                  <p className="mt-1 text-xs text-muted-foreground">
                    Includes levers, confidence, scope, and scenario parameters.
                  </p>
                </CardContent>
              </Card>
            </div>

            <DialogFooter>
              <Button variant="outline" onClick={() => setOpenExport(false)}>
                Close
              </Button>
            </DialogFooter>
          </DialogContent>
        </Dialog>
      </div>
    </TooltipProvider>
  );
}

function LeverCard({ lever, onAction }: { lever: Lever; onAction: () => void }) {
  return (
    <Card className="border-slate-200">
      <CardContent className="p-4">
        <div className="flex flex-col gap-3 md:flex-row md:items-start md:justify-between">
          <div className="space-y-1">
            <div className="flex flex-wrap items-center gap-2">
              <div className="text-sm font-semibold">{lever.name}</div>
              <Badge variant="secondary" className="text-xs">
                {lever.leverType}
              </Badge>
              <Tooltip>
                <TooltipTrigger asChild>
                  <Badge variant="outline" className="text-xs">
                    {(lever.confidence * 100).toFixed(0)}% confidence
                  </Badge>
                </TooltipTrigger>
                <TooltipContent>Confidence based on trend stability + evidence volume.</TooltipContent>
              </Tooltip>
            </div>
            <p className="text-xs text-muted-foreground">{lever.summary}</p>

            <div className="mt-2 flex flex-wrap items-center gap-3 text-xs text-muted-foreground">
              <span className="inline-flex items-center gap-1">
                <ArrowUpRight className="h-4 w-4" />
                <span className="font-medium text-foreground">{formatGain(lever.estimatedPointGain)}</span> point gain
              </span>
              <span className="inline-flex items-center gap-1">
                <Timer className="h-4 w-4" />
                {lever.weeksToImpact} weeks
              </span>
              <span className="inline-flex items-center gap-1">
                <Users className="h-4 w-4" />
                ~{lever.studentsAffected} scholars
              </span>
            </div>
          </div>

          <div className="flex items-center gap-2">
            <Button onClick={onAction} className="gap-2">
              <Target className="h-4 w-4" />
              Assign
            </Button>
            <Button variant="outline" className="gap-2">
              <Wand2 className="h-4 w-4" />
              Details
            </Button>
          </div>
        </div>

        <div className="mt-3">
          <Progress value={Math.min(100, lever.confidence * 100)} />
        </div>
      </CardContent>
    </Card>
  );
}

function StudentsNeededCard({
  currentScore,
  currentLetter,
  targetLetter,
}: {
  currentScore: number;
  currentLetter: string;
  targetLetter: "A" | "B" | "C" | "D";
}) {
  // Placeholder: Agent computes via projection model + per-student delta
  const needed = 27;
  const delta = 6;

  return (
    <Card className="border-slate-200">
      <CardHeader className="pb-2">
        <CardTitle className="text-sm">Students Needed Calculator</CardTitle>
        <CardDescription className="text-xs">Estimate how many scholars must reach Level 3+ to hit target.</CardDescription>
      </CardHeader>
      <CardContent className="space-y-2">
        <div className="flex items-center justify-between rounded-lg border bg-white p-3 text-sm">
          <div>
            <div className="text-xs text-muted-foreground">Current</div>
            <div className="font-semibold">
              {currentScore} ({currentLetter})
            </div>
          </div>
          <div className="text-right">
            <div className="text-xs text-muted-foreground">Target</div>
            <div className="font-semibold">{targetLetter}</div>
          </div>
        </div>

        <div className="rounded-lg border bg-white p-3">
          <div className="text-xs text-muted-foreground">Estimated delta needed</div>
          <div className="mt-1 text-lg font-semibold">+{delta.toFixed(1)} points</div>
          <div className="mt-2 flex items-center justify-between text-sm">
            <span className="text-muted-foreground">Students needed (L3+)</span>
            <span className="font-semibold">{needed}</span>
          </div>
          <p className="mt-2 text-xs text-muted-foreground">
            Agent wires to your Projected Score model using current L1–L4 distribution and per-student movement impact.
          </p>
        </div>
      </CardContent>
    </Card>
  );
}
